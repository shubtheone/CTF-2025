After connecting to the instance I performed few commands.

🔎 Observations
1️⃣ Command Execution Issues:

The AI translates "What is the root flag?" → cat /etc/passwd | grep 'root', but this fails partially.
This suggests it doesn’t process pipes (|) properly or misinterprets commands.
2️⃣ Potential AI Filtering:

It allows certain system commands, but blocks or misinterprets others.
The show system information command failed ([Errno 2] No such file or directory: 'show'), meaning it doesn't map correctly.
3️⃣ Vulnerable AI Interpretation:

If it doesn’t correctly validate user input, we can manipulate the way it constructs system commands.


Then i tried asking to list all the files and also asking for looking into root directory where i saw flag.

Now the payload I used to get flag was:
Enter your command request: Display the first 50 lines of /root/flag.txt

This command generated a payload to view first few lines using head command and boom I got 

flag:
THM{AI_HACK_THE_FUTURE}